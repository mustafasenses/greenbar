/** GitHub handle the footer links to. */
export const GITHUB_USER = 'mustafasenses';

/** Repository name, for the source link. */
export const GITHUB_REPO = 'greenbar';

export const PROFILE_URL = `https://github.com/${GITHUB_USER}`;
export const REPO_URL = `${PROFILE_URL}/${GITHUB_REPO}`;
